// import * as fs from 'fs';
// import { processResponse } from '../response';

describe('enquiry-response', () => {
  it('should unzip response', async () => {
    // const responseResult = fs.readFileSync('src/__tests__/response.txt').toString();
    // const response = {
    //   response: responseResult,
    //   statusCode: 200,
    // };
    // const result = await processResponse(response);
    // const correctResult = fs.readFileSync('src/__tests__/responseResult.txt').toString();
    // expect(result.result).toEqualXML(correctResult);
  });
});
